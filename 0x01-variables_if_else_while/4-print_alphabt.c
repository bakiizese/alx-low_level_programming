#include <stdio.h>
#include <stdlib.h>
/**
 * main - Entry point
 * Return: Always 0 (seccess)
 */
int main(void)
{
	char chr = 'a';

	while (chr <= 'z')
	{
		if ((chr != 'q' && chr != 'e')&& c <= 'z'))
			putchar(chr);
		chr++;
	}
	putchar('\n');
	return (0);
}
